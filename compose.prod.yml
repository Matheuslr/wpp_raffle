services:
  poker-draw-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: poker-draw-bot
    restart: unless-stopped
    user: "1000:1000"
    env_file:
      - .env
    volumes:
      - ../runtime/auth:/app/runtime/auth
      - ../runtime/data:/app/runtime/data
